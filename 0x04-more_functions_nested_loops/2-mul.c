#include "main.h"
/**
 * mul - multiplies two intergers
 * @x: The first number to multiply with
 * @y: The second number to multiply with the first
 * Return: The result after multiplication
 */
int mul(int x, int y)
{
return (x * y);
}
